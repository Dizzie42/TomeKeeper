# --- LangChain core + integrations ---
# Supports both 0.3 layout AND 1.x layout (via langchain-classic).
# Our retrieval imports try the 0.3 path first, then fall back to
# langchain_classic.* paths. This makes the project portable across
# the major version boundary that landed in late 2025.
langchain>=0.3.20
langchain-core>=0.3.30
langchain-community>=0.3.15
langchain-ollama>=0.2.2
langchain-qdrant>=0.2.0
langchain-text-splitters>=0.3.2
# Only used when langchain >= 1.0 is installed; harmless otherwise.
langchain-classic>=0.1.0; python_version >= "3.10"

# --- Vector DB client ---
qdrant-client>=1.12.1

# --- Web layer ---
fastapi>=0.115.5
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.3

# --- Frontend ---
streamlit>=1.40.1

# --- Config / utils ---
# Python 3.14 requires newer pydantic for PEP 649 annotation handling.
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.1

# --- Document loading ---
pymupdf>=1.24.13

# --- HTTP clients ---
httpx>=0.27.2
requests>=2.32.3

# --- Phase 2: hybrid retrieval + reranker ---
rank-bm25>=0.2.2              # BM25 keyword retriever
sentence-transformers>=3.3.1  # Cross-encoder reranker (BAAI/bge-reranker-base)

# --- Phase 2: Notion ingestion ---
notion-client>=2.2.1

# --- Testing ---
pytest>=8.3.3
